node_modules/
dist/
coverage/
.playwright/
*.log

# Local environment files
.env
.env.local
.env.*.local

# Local opencode config & skills (opencode.json contains a Render MCP token)
.opencode/
opencode.json

.DS_Store

# Local wallet credentials (never commit private keys)
wallet*.json

# Runtime event store
events.json
test-events-*.json
